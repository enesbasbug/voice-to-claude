{
  "hooks": {
    "PostStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"import sys; sys.path.insert(0, '${CLAUDE_PLUGIN_ROOT}/src'); from voice_to_claude.daemon import start_daemon, is_daemon_running; from voice_to_claude.config import Config; c = Config.load(); exit(0) if not c.setup_complete or is_daemon_running() else start_daemon(background=True, quiet=True)\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
